const $menu = $('#navbarSupportedContent');
    // const $trigger = $("button#buttonToggle");
    // for click outlide to close navbar. 
    // $(document).on('mouseup',function(e){
    //     if(!$menu.is(e.target) // if the target of the click isn't the container...
    //     && $menu.has(e.target).length === 0 && !$trigger.has(e.target).length) // ... nor a descendant of the container
    //     {
    //       $menu.removeClass('toggolenav');
    //     }
    // }); 